{
  "name": "real-estate-crawler",
  "version": "1.0.0",
  "description": "Crawls assortment of real estate APIs to provide a consisten view of search results",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jose Quijada",
  "license": "ISC",
  "devDependencies": {
    "jest": "^24.8.0",
    "jest-junit": "^6.4.0"
  },
  "scripts": {
    "test": "jest --config=\"./jest.config.js\" --coverage"
  }
}
